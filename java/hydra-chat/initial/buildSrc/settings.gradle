pluginManagement {
    plugins {
        id 'com.github.spotbugs' version "${pluginVersionSpotBugs}"
    }
    repositories {
        gradlePluginPortal {
            content {
                excludeGroupByRegex "com\\.weareadaptive.*"
            }
        }
    }
}
