package operations;

public class AddOperation implements Operation {
    @Override
    public Object process(Object a, Object b) {
        // how do I know if objects can be added?
        if (a instanceof Number)


        return null;
    }
}
